# Smooth Language Experiments

## Ideas:
* Test distance of more examples across layers
* Compare to token's input embedding.
* Test distance of position embedding across layer (should we decorrelate in training?).